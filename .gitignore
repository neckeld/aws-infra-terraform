# Terraform - Arquivos de estado e plugins (não devem ir para o Git)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Segurança - Chaves privadas (NUNCA suba arquivos .pem!)
*.pem

# LocalStack - Dados temporários
.localstack/

# Sistema - Arquivos inúteis do Windows
Thumbs.db
desktop.ini